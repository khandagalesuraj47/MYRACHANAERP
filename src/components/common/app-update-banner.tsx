import React from 'react'

/**
 * AppUpdateBanner
 *
 * NOTE: Full-screen intrusive popups on app launch have been eliminated as per user request.
 * All updates are now managed cleanly and on-demand inside the dedicated "Settings & Updates" tab
 * (src/components/common/app-settings-view.tsx).
 */
export function AppUpdateBanner() {
  return null
}
